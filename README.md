# usersAPI
Random users RESTful API that handles CRUD operations using MySQL database hosted locally.

## Technologies used
- ExpressJS
- MySQL

## Endpoints
```
GET     l/users
GET     /users/:id
POST    /users
PUT     /users/:id
DELETE  /users/:id
```
